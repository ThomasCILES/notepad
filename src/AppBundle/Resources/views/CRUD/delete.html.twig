{% extends '@App/layout.html.twig' %}

{% block body %}
    <h1>{{ 'app.ui.crud.delete.title'|trans }}</h1>

    <div>
        {{ form_start(form) }}
        {{ form_widget(form) }}
        <button type="submit">{{ 'app.ui.crud.delete.submit'|trans }}</button>
        {{ form_end(form) }}
    </div>
{% endblock %}