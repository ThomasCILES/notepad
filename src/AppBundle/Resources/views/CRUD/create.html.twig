{% extends '@App/layout.html.twig' %}

{% block content %}
    <h1>{{ 'app.ui.crud.create.title'|trans }}</h1>

    <div>
        {{ form_start(form) }}
        {{ form_widget(form) }}
        <button type="submit" class="btn btn-primary">{{ 'app.ui.crud.create.submit'|trans }}</button>
        {{ form_end(form) }}
    </div>
{% endblock %}